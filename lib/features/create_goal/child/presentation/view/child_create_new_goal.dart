import 'package:flutter/material.dart';

class ChildCreateNewGoal extends StatelessWidget {
  const ChildCreateNewGoal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}